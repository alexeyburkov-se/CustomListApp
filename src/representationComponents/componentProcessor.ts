export enum NodeStringVariants {
  Text = "text",
  Label = "label",
  Title = "title",
  Rating = "rating",
}

export enum NodeBooleanVariants {
  CheckBox = "checkBox",
}

export enum NodeNumberVariants {
  Rating = "rating",
}

export enum NodeRatioVariants {
  RatingRatio = "ratingRation",
}
